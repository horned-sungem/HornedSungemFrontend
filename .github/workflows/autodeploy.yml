name: autodeploy

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  autodeploy:
    name: Deploying to gh-pages
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js for use with actions
        uses: actions/setup-node@v1.1.0
        with:
          version:  12.x
      - name: Checkout branch
        uses: actions/checkout@v2

      - name: Clean install dependencies
        run: yarn ci

      - name: Run deploy script
        run: |
          git config user.name ${{secrets.USER_NAME}} && git config user.email ${{secrets.USER_EMAIL}}
          yarn deploy 